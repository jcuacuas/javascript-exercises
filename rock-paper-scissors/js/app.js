function add7(number) {
  return x = number + 10
}

function capitalize(string) {
  return capilizedWord = string.charAt(0).toUpperCase();
}

function lastLetter(word) {
  return grabLetter = word.charAt(word.length - 1);
}

document.getElementById("testing").innerHTML = lastLetter("chicken");
